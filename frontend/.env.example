# No map API key required — the map uses keyless CARTO/Esri raster tiles.
#
# Base-map style: "dark" (default, CARTO dark) or "satellite" (Esri imagery)
VITE_MAP_STYLE=dark

# Optional override for the backend WebSocket URL
# VITE_WS_URL=ws://localhost:8765
